# 🏗️ Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER'S BROWSER                          │
│                      http://localhost:3000                      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 │ HTTP Requests
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (PORT 3000)                         │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Static Website (HTML/CSS/JavaScript)                      │ │
│  │                                                             │ │
│  │  • index.html          - Main HTML structure              │ │
│  │  • css/style.css       - All styling                      │ │
│  │  • js/config.js        - API configuration                │ │
│  │  • js/main.js          - Interactive functionality        │ │
│  └───────────────────────────────────────────────────────────┘ │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 │ API Calls (fetch)
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                     BACKEND (PORT 5000)                         │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Flask REST API                                            │ │
│  │                                                             │ │
│  │  Endpoints:                                                │ │
│  │  • GET  /api/health      - Health check                   │ │
│  │  • POST /api/contact     - Contact form                   │ │
│  │  • POST /api/chatbot     - AI assistant                   │ │
│  │  • GET  /api/projects    - Projects list                  │ │
│  │                                                             │ │
│  │  Features:                                                 │ │
│  │  • Email integration (Flask-Mail)                         │ │
│  │  • CORS enabled (Flask-CORS)                              │ │
│  │  • Chatbot knowledge base                                 │ │
│  │  • Error handling & logging                               │ │
│  └───────────────────────────────────────────────────────────┘ │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 │ SMTP
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                            │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Email Provider (Gmail)                                    │ │
│  │  • Sends contact form emails                              │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## Component Interaction Flow

### 1. User Visits Website
```
User → Browser → http://localhost:3000 → Frontend Server → index.html
```

### 2. Chatbot Interaction
```
User types message
    ↓
Frontend (main.js) sends POST to /api/chatbot
    ↓
Backend (app.py) processes with CHATBOT_KNOWLEDGE
    ↓
Returns response
    ↓
Frontend displays in chat window
```

### 3. Contact Form Submission
```
User fills form
    ↓
Frontend validates and sends POST to /api/contact
    ↓
Backend receives data
    ↓
Flask-Mail sends email via SMTP
    ↓
Returns success/error
    ↓
Frontend shows confirmation message
```

### 4. Projects Loading
```
Page loads
    ↓
Frontend sends GET to /api/projects
    ↓
Backend returns projects array
    ↓
Frontend dynamically creates project cards
```

---

## Technology Stack

### Frontend Layer
```
┌─────────────────────────────────────┐
│  HTML5                              │
│  ├─ Semantic structure              │
│  ├─ Accessibility features          │
│  └─ SEO meta tags                   │
├─────────────────────────────────────┤
│  CSS3                               │
│  ├─ CSS Variables (theming)         │
│  ├─ Flexbox & Grid layouts          │
│  ├─ Animations & transitions        │
│  └─ Responsive design (mobile-first)│
├─────────────────────────────────────┤
│  JavaScript ES6+                    │
│  ├─ Async/Await (API calls)         │
│  ├─ DOM manipulation                │
│  ├─ Event handling                  │
│  └─ Form validation                 │
├─────────────────────────────────────┤
│  External Libraries                 │
│  ├─ AOS (Animate On Scroll)         │
│  ├─ Font Awesome (Icons)            │
│  └─ Google Fonts                    │
└─────────────────────────────────────┘
```

### Backend Layer
```
┌─────────────────────────────────────┐
│  Python 3.8+                        │
│  └─ Modern Python features          │
├─────────────────────────────────────┤
│  Flask Framework                    │
│  ├─ Routing                         │
│  ├─ Request/Response handling       │
│  ├─ JSON API                        │
│  └─ Error handlers                  │
├─────────────────────────────────────┤
│  Flask Extensions                   │
│  ├─ Flask-CORS (Cross-origin)       │
│  ├─ Flask-Mail (Email)              │
│  └─ python-dotenv (Config)          │
├─────────────────────────────────────┤
│  Production Server                  │
│  └─ Gunicorn (WSGI server)          │
└─────────────────────────────────────┘
```

---

## Data Flow Diagrams

### Contact Form Flow
```
┌─────────────┐
│    User     │
│ Fills Form  │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│   Frontend      │
│ Validates Input │
└──────┬──────────┘
       │ POST /api/contact
       │ { name, email, message }
       ▼
┌──────────────────┐
│    Backend       │
│ Receives Request │
└──────┬───────────┘
       │
       ├──► Validates data
       │
       ├──► Creates email message
       │
       ├──► Sends via Flask-Mail
       │        │
       │        ▼
       │   ┌─────────────┐
       │   │ Gmail SMTP  │
       │   └─────────────┘
       │
       ▼
   Returns JSON
   { success: true }
       │
       ▼
┌──────────────────┐
│    Frontend      │
│ Shows Success    │
└──────────────────┘
```

### Chatbot Flow
```
┌─────────────┐
│    User     │
│ Types Query │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│   Frontend      │
│ Captures Input  │
└──────┬──────────┘
       │ POST /api/chatbot
       │ { message: "what are your skills?" }
       ▼
┌──────────────────────┐
│      Backend         │
│ CHATBOT_KNOWLEDGE    │
└──────┬───────────────┘
       │
       ├──► Converts to lowercase
       │
       ├──► Searches for keywords
       │    in knowledge base
       │
       ├──► Matches category
       │    (e.g., "skills")
       │
       ▼
   Returns response
   { success: true,
     response: "Ikrame is proficient in..." }
       │
       ▼
┌──────────────────┐
│    Frontend      │
│ Displays Reply   │
└──────────────────┘
```

---

## File Organization

### Frontend Structure
```
frontend/
│
├── index.html              Main HTML file
│   ├── <head>             Meta tags, fonts, CSS links
│   ├── <nav>              Navigation bar
│   ├── <section>          Hero, About, Projects, etc.
│   └── <footer>           Footer section
│
├── css/
│   └── style.css          All styles
│       ├── Variables      Color scheme, fonts
│       ├── Global         Reset, typography
│       ├── Components     Buttons, cards, forms
│       ├── Sections       Hero, about, projects
│       └── Responsive     Media queries
│
└── js/
    ├── config.js          API configuration
    │   ├── API_CONFIG     Base URL, endpoints
    │   └── apiCall()      HTTP request helper
    │
    └── main.js            Main functionality
        ├── AOS init       Animation setup
        ├── Navigation     Menu, scroll effects
        ├── Projects       Load & display
        ├── Testimonials   Slider logic
        ├── Contact Form   Submit handler
        └── Chatbot        Chat interface
```

### Backend Structure
```
backend/
│
├── app.py                 Main Flask application
│   ├── Config             Flask & email setup
│   ├── Routes
│   │   ├── /api/health           Health check
│   │   ├── /api/contact          Contact form
│   │   ├── /api/chatbot          AI assistant
│   │   └── /api/projects         Projects list
│   ├── Helper Functions
│   │   └── get_bot_response()   Chatbot logic
│   └── Error Handlers    404, 500 errors
│
├── requirements.txt       Python dependencies
│   ├── Flask==3.0.0
│   ├── Flask-CORS==4.0.0
│   ├── Flask-Mail==0.9.1
│   └── ...
│
└── .env                   Configuration (not in git)
    ├── FLASK_ENV
    ├── PORT
    ├── MAIL_USERNAME
    └── ...
```

---

## Deployment Architecture

### Development (Local)
```
┌──────────────────────────────────────┐
│         Local Machine                │
│                                      │
│  Frontend: localhost:3000            │
│  Backend:  localhost:5000            │
│                                      │
│  Both running via PowerShell scripts │
└──────────────────────────────────────┘
```

### Production (Recommended)
```
┌─────────────────────────────────────────┐
│         Frontend (Netlify/Vercel)       │
│         https://yourname.netlify.app    │
└──────────────┬──────────────────────────┘
               │
               │ API Calls
               ▼
┌─────────────────────────────────────────┐
│         Backend (Railway/Render)        │
│         https://api.yourname.com        │
└──────────────┬──────────────────────────┘
               │
               │ SMTP
               ▼
┌─────────────────────────────────────────┐
│         Email Service (Gmail)           │
└─────────────────────────────────────────┘
```

### Docker Deployment
```
┌─────────────────────────────────────────┐
│         Docker Host (VPS/Cloud)         │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  Frontend Container               │ │
│  │  Port: 3000 → 80                  │ │
│  │  Nginx serving static files       │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  Backend Container                │ │
│  │  Port: 5000                       │ │
│  │  Gunicorn + Flask                 │ │
│  └───────────────────────────────────┘ │
│                                         │
│  Docker Network: portfolio-network     │
└─────────────────────────────────────────┘
```

---

## Security Layers

```
┌─────────────────────────────────────────┐
│         Security Measures               │
├─────────────────────────────────────────┤
│  1. Environment Variables               │
│     • .env not in version control       │
│     • Secrets in env vars               │
├─────────────────────────────────────────┤
│  2. CORS Configuration                  │
│     • Controlled origins                │
│     • Allowed methods                   │
├─────────────────────────────────────────┤
│  3. Input Validation                    │
│     • Frontend validation               │
│     • Backend validation                │
│     • Email format checks               │
├─────────────────────────────────────────┤
│  4. Email Security                      │
│     • App passwords (not main pwd)      │
│     • 2FA enabled                       │
│     • TLS encryption                    │
├─────────────────────────────────────────┤
│  5. Error Handling                      │
│     • No sensitive info in errors       │
│     • Logging for debugging             │
│     • Graceful failures                 │
└─────────────────────────────────────────┘
```

---

## Performance Optimizations

```
Frontend:
✓ Minified CSS in production
✓ Lazy loading for images
✓ Async script loading
✓ Optimized animations
✓ Responsive images

Backend:
✓ Gunicorn workers
✓ Efficient routing
✓ Connection pooling
✓ Error caching
✓ Logging optimization

Deployment:
✓ CDN for static assets
✓ Gzip compression
✓ HTTP/2 support
✓ SSL/TLS encryption
✓ Caching headers
```

---

**This architecture provides a solid foundation for a professional portfolio website!** 🚀
